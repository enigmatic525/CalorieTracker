name: Inject Secret
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inject API Key
        run: sed -i "s/API_KEY/${{ secrets.API_KEY }}/g" index.html
